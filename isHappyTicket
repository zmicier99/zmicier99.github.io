const isHappyTicket = (num) => {
  const str = String(num);
   
  if ((str[0]*1+ str[1]*1+str[2]*1)===(str[3]*1+ str[4]*1+str[5]*1))
  return true;
  else return false;
  
  }
  
  isHappyTicket(128722);
