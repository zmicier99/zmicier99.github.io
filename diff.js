const diff = (integer1, integer2) => {
  const circle = 360;
  let int1=integer2-integer1;
  let int2=circle-integer2-integer1;
  let result;
  
  if (int1<int2 && int2>0) 
  return result = int1;
  else return result = int2;
  
  return result;
  } 
  
  diff (0,45);
